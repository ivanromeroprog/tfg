{% extends 'base.html.twig' %}
{% block menu %}
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">
                    👨‍🏫
                    Edu-track</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    
                    {#
                    {% if is_granted('ROLE_USER') %}
                        <ul class="nav navbar-nav navbar-right">
                            <li class="nav-item disabled">
                                    <a class="nav-link{% if app.request.attributes.get( '_route' ) starts with 'app_mi_cuenta' %} active{% endif %}" href="{{ path('app_mi_cuenta') }}" title="Mis Cuenta"><i class="bi bi-person-fill "></i> {{ app.user.username }}</a>
                            </li>
                            <li class="nav-item">

                                <a class="nav-link" href="{{ path('app_logout') }}">Salir</a>
                            </li>
                        </ul>
                    {% else %}
                        <ul class="nav navbar-nav navbar-right">
                            <li class="nav-item">
                                <a class="nav-link{% if app.request.attributes.get( '_route' ) starts with 'app_login' %} active{% endif %}" href="{{ path('app_login') }}">Iniciar Sesión</a>
                            </li>
                        </ul>
                    {% endif %}
                    #}
                    
                </div>
            </div>
        </nav>
{%  endblock %}
